<UserControl x:Class="VKPlay.VKPlaySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel Margin="20">
        <CheckBox DockPanel.Dock="Top" Name="CheckVKPlayImportInstalled"
            Margin="0,0,0,15"      
            IsChecked="{Binding Settings.ImportInstalledGames}"
            Content="{DynamicResource LOCVKPlaySettingsImportInstalledLabel}"/>

        <CheckBox DockPanel.Dock="Top" Name="CheckVKPlayConnectAccount"
            Margin="0,0,0,15"      
            IsChecked="{Binding Settings.ConnectAccount}"
            Content="{DynamicResource LOCVKPlaySettingsConnectAccount}"/>
        
    </StackPanel>
</UserControl>